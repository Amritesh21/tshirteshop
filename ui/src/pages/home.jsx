const { Typography } = require("@mui/material")

const HomePage = () => {
    return (
        <Typography variant="h2">
            Hello world 2
        </Typography>
    )
}
export default HomePage;